#include<stdio.h>
int main()
{
    int a,b,t,i,j,arr[0];
    scanf("%d",&t);
    while(t--)
    {
        scanf("%d",&a);
        for(i=0; i<a; i++)
        {
            scanf("%d",arr[i]);
        }

    }

    return 0;
}
