#include<bits/stdc++.h>
#define optimize() ios_base::sync_with_stdio(0);cin.tie(NULL);cout.tie(NULL);
#define endl '\n'
using namespace std;
int main()
{
    optimize();
        string c;
        cin>>c;
            if((c=='A' || c=='E' || c=='I' || c=='O' || c=='U')){
                cout<<"vol"<<endl;
            }
        cout<<"con"<<endl;
    return 0;
}
